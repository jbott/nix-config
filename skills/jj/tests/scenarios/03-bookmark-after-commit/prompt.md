You just finished committing work on a new search service. The stack is two
commits deep on top of trunk, with `@` empty.

**Your task:** create a `john/<name>` bookmark for this work, following the
skill's naming convention. The bookmark should point at the most recent
real commit (not the empty working copy).

When done, briefly report:
1. The exact `jj` command you ran
2. The bookmark name you chose
3. Which commit it points at
