You just committed `services/api: add health check` at `@-`. Then you
realized you forgot to add error handling, so you edited the file in the
working copy. Now `@` has the error-handling changes but no description.

**Your task:** fold the `@` changes into `@-` so there's a single combined
commit, with `@` clean afterwards.

When done, briefly report:
1. The exact `jj` command you ran
2. The new description on `@-`
