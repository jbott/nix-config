You have changes in `@` adding input validation to `charge()`. **Your
environment has `JJ_EDITOR` set to a script that errors immediately** — any
`jj` command that tries to open the description editor will fail with a
clear message instead of hanging.

**Your task:** commit the changes successfully with a proper project-prefix
description. If your first attempt fails because of the editor trap, read
the error message and recover.

When done, briefly report:
1. The exact `jj` command(s) you ran (including any that failed)
2. The error message the trap produced (if it fired)
3. The final commit description
