You just finished a feature and want to publish it. The stack has two
commits and a `john/add-email-notifier` bookmark at the tip. Origin is
configured.

**Your task:** push this stack to origin so others can see it. Use the
skill's preferred command for pushing.

When done, briefly report:
1. The exact `jj` command(s) you ran
2. Confirmation that the bookmark exists on origin
